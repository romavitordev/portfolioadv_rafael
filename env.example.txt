# Servidor
PORT=3001
NODE_ENV=development
FRONTEND_URL=http://localhost:3001

# SQL Server
DB_SERVER=localhost
DB_PORT=1433
DB_NAME=RafaelPedrosoAdv
DB_USER=sa
DB_PASSWORD=troque-esta-senha
DB_ENCRYPT=false
DB_TRUST_SERVER_CERTIFICATE=true

# Administracao da API
# Use esta chave no header x-admin-key para rotas internas:
# GET /api/contatos, GET/POST /api/agendamentos, GET/POST /api/depoimentos
ADMIN_API_KEY=troque-por-uma-chave-longa

# E-mail de notificacao de novos leads
SMTP_HOST=smtp.seudominio.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=contato@seudominio.com
SMTP_PASS=senha-do-email
EMAIL_TO=contato@seudominio.com
